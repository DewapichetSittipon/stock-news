# LINE Messaging API — TEMPLATE ONLY. Do NOT put real values here (this file is committed).
# Copy to .env (git-ignored) and fill that in, or set these as GitHub Actions secrets.
LINE_CHANNEL_ACCESS_TOKEN=
LINE_USER_ID=

# Optional: base URL of the Cloudflare live-quote proxy (see worker/README.md).
# Enables the live price overlay on daily-mode cards. Unset = EOD prices.json.
VITE_QUOTE_PROXY_URL=
